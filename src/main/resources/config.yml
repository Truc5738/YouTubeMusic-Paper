# YouTubeMusic 2.12.0 — API Edition + Spotify-style UI + private synced lyrics
lyrics:
  enabled: true
  directory: "lyrics"
  format: "lrc"
  private-chat: true

ui:
  enabled: true
  bedrock-forms: true
  java-inventory: true

queue:
  max-size: 25
  loop: false
  shuffle: false
  radio: false

request:
  cooldown-seconds: 5

security:
  max-url-length: 2048
  max-track-duration-seconds: 3600
  allow-youtube-only: true

player:
  default-volume: 80
  max-volume: 100

storage:
  file: music.db

audio:
  backend: api-resource-pack
  max-duration-seconds: 3600
  default-duration-seconds: 600
  api:
    # Audio API chạy trên TCP 26467. UDP 26467 vẫn dành cho Geyser/Voice Chat.
    # Đổi host nếu địa chỉ máy chủ của bạn khác.
    url: "http://nvnmc.asia:26467/"
    key: ""
    bitrate: "128"
    timeout-seconds: 120
  youtube-data-api-key: ""
  download-timeout-seconds: 300
  resource-pack-url-template: "http://YOUR_PUBLIC_IP:8126/packs/%s"
  resource-pack-required: false
  resource-pack-timeout-seconds: 90
  resource-pack-https-recommended: true
  play-delay-ticks: 60
  resource-pack-server:
    enabled: true
  resource-pack-server-port: 8126
  cache-directory: "audio-cache"
